# Archived ALG diagnostics

These files record older researcher-sync/Ours-common diagnostics only. They
are not executable canonical ALG entry points. The active ALG commands are the
dataset `train.py` files under `methods/ALG/`; the runtime rejects
`draft_common`, researcher derivative normalization, strict `>` stopping, and
a controller-only 20-epoch delay.
